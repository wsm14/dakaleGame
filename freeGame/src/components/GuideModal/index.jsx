import React from 'react';
import { Mask } from 'antd-mobile';
import './index.less';

function index() {
  const modalProps = {
    visible: true,
    opacity: 0.8,
  };

  return <>{/* <Mask {...modalProps}></Mask> */}</>;
}

export default index;
